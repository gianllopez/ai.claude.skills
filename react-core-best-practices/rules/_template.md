---
title: <Title>
impact: <CRITICAL | HIGH | MEDIUM | LOW>
description: <One-line summary of the rule>
tags: <comma, separated, tags>
---

## <Title>

**Impact (<LEVEL>):** <Why this matters — the concrete cost of getting it wrong, on any platform>

<Optional paragraph: what breaks when the rule is ignored, stated once for both platforms>

**Guidelines:**

1.  **<Topic>:**
    - <directive>

**Incorrect (<what is wrong, and why it is wrong on both platforms>):**

```tsx
// Bad: <the defect>
```

**Correct (React DOM):**

```tsx
// Good: <the fix in DOM idiom>
```

**Correct (React Native):**

```tsx
// Good: <the same fix in React Native idiom>
```

Reference: [<label>](url)
